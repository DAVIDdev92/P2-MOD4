function createHeaderElement(header) {
    const element = document.createElement("header");
    element.className = "header";
    element.textContent = Header;
    return element;
}

function createRankElement(rank) {
    const element = document.createElement("rank");
    element.className = "rank";
    element.textContent = rank;
    return element;
}

function createPhotoElement(photo) {
    const element = document.createElement("photo");
    element.className = "photo";
    element.textContent = photo;
    return element;
}

function createTitleElement(title) {
    const element = document.createElement("title");
    element.className = "title";
    element.textContent = title;
    return element;
}

function createAverageRatingElement(averageRating) {
    const element = document.createElement("average-rating");
    element.className = "average-rating";
    element.textContent = average-rating;
    return element;
}

function createNumReviewsElement(numReviews) {
    const element = document.createElement("num-reviews");
    element.className = "num-reviews";
    element.textContent = num-reviews;
    return element;
}

function createYourRatingElement(yourRating) {
    const element = document.createElement("your-rating");
    element.className = "your-rating";
    element.textContent = your-rating;
    return element;
}
